/**
 * Base Data Transfer Object interface placeholder.
 * Add feature-specific DTOs here (e.g., product.dto.ts).
 */
export interface BaseDto {
  id: string;
  createdAt: string;
  updatedAt: string;
}
